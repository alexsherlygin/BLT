global:
  resolve_timeout: 5m

route:
  receiver: default-receiver
  group_by: ["alertname", "job", "instance", "severity"]
  group_wait: 5s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default-receiver

templates: []
